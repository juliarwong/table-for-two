import React from 'react';

function Header() {
    return (
        <header>
        <h1>Table for Two</h1>
        <p>Find a date spot in your city!</p>
        </header>
    );
}

export default Header;
