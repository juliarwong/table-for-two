const Form = () => {
    return (
        <form>
        <label htmlFor="city">enter a city:</label>
        <input type="text" id="city" />
        <button>Submit</button>
        </form>
    );
};

export default Form;